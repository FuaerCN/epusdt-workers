{
  "name": "epusdt-worker",
  "version": "1.0.0",
  "description": "epusdt ported to Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "hono": "^4.0.0"
  }
}
